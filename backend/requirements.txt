# Breadboard Simulator — backend dependencies
# Install:  python -m venv venv && venv/Scripts/activate  (Windows)
#           pip install -r requirements.txt
# Run:      uvicorn main:app --reload

fastapi==0.137.0
uvicorn[standard]==0.49.0
numpy==2.4.6
pydantic==2.13.4

# Dev / tests
pytest==8.3.4
httpx==0.28.1
